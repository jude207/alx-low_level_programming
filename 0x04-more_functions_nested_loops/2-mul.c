#include "main.h"

/**
 * mul- multiplies two number
 * @a: input first parameter
 * @b: input second parameter
 * Return: returns result
 */

int mul(int a, int b)
{
	int m;

	m = a * b;
	return (m);
}
