#include "main.h"
#include <stdio.h>

/**
 *
 *
 *
 *
 */

void reverse_array(int *a, int n)
{
	int i;
	int st = 0;

	*(a + 0) = *(a - j);

	for (i = 0; i < n; i++)
	{
		printf("%p", ptr);
		ptr++;
	}
}	
