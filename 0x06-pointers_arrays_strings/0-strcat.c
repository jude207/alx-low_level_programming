#include "main.h"
#include <string.h>

/**
 * _strcat - this function join two words
 * @dest: first param
 * @src: second param
 * Return: returns string
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
