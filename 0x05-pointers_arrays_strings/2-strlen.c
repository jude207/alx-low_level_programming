#include "main.h"
#include <string.h>

/**
 * _strlen - function to get lenght of any sting
 * @s: string pointer as parameter to fun
 * Return: returns lenght
 */

int _strlen(char *s)
{
	return (strlen(s));
}
