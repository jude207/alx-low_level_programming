#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 *
 *
 *
 */

char *_memset(char *s, char b, unsigned int n)
{
	/**unsigned int i;*/

	return (memset(s, b, n));

	/**for (i = 0; i < n; i++)
	{
		printf("%c", *(s + n));
	}
	printf("\n");
	return (0);*/
}
